# Use postgres/example user/password credentials
services:
  web:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    ports: 
      - 4008:4008
    command: [
      "sh",
      "-c",
      "mix deps.get && mix phx.server"
    ]
    environment:
      MIX_ENV: dev